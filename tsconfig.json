{
  "compilerOptions": {
    "target": "ES2020",         /* Define a versão do JavaScript de saída */
    "module": "CommonJS",       /* Usa o sistema de módulos do Node.js (CommonJS) */
    "esModuleInterop": true,    /* Permite usar import * as ... from '...' com CommonJS */
    "forceConsistentCasingInFileNames": true,
    "strict": true,             /* Ativa as verificações rigorosas de tipo (Recomendado) */
    "skipLibCheck": true,
    "outDir": "./dist"          /* Onde o TypeScript colocará os arquivos compilados (opcional) */
  }
}